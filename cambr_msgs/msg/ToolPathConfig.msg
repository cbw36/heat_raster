float64 run_density        # spacing between points on a single segment
float64 offset_spacing     # spacing between neighboring raster strokes
float64 height_offset      # TCP z-offset from surface
float64 min_hole_size      # holes larger than this will split a segment
float64 min_segment_size   # small path segments will be dropped
